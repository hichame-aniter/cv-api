services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cv-api-backend
    ports:
      - "8080:8080"
    # volumes:
    #   - ./data:/root/data   # Mount local data folder
    # environment:
    #   - DATA_PATH=/root/data/cv.json  # Optional: override inside app
    restart: unless-stopped